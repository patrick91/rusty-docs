def a_function(a: int) -> int:
    """A function that returns the input value plus one.

    This is an example continuation of description.
    I still think we should merge title and description.
    First line of the docstring being a title is a bit odd. Maybe.

    🤔

    Args:
        a: The input value.

    Returns:
        The input value plus one.
    """
    return a + 1
